services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"

  ingestion:
    build: ./ingestion
    depends_on:
      - postgres
    volumes:
      - ./data:/app/data
